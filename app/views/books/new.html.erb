<h1>User</h1>
<%= form_with model: @books, local:true do |f| %>
  <h4>New book</h4>

  <h4>Title</h4>
  <%= f.text_field :shop_name %>
  <h4>Option</h4>
  <%= f.text_area :caption %>
  <%= f.submit 'Create Book' %>
<% end %>