<% if @user.errors.any? %>
  <%= @user.errors.count %>error prohibited this obj from being saved:
  <% @user.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
<% end %>
<p id="notice"></p>
 <div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
    <h2>User info</h2>
<%= form_with model:@user, local:true do |f| %>
  <div for="inputName">Name</div>
  <%= f.text_field :name, autofocus: true, id:"inputName", placeholder:"名前"%>
  <div>image</div>
  <%= f.attachment_field :profile_image,autofocus: true, id:"inputimage", placeholder:"ファイルの選択"%>
  <div>introduction</div>
  <%= f.text_area :introduction, autofocus: true, id:"inputName"%>
  <div><%= f.submit "Update User", class: "btn btn-secondary" %></div>
  <% end %>
    </div>
   </div>
 </div>


